 {% for cat in categories %}
     <tr>
    <td>{{  cat.id}}</td>
    <td>{{ cat.libelle }}</td>

    <td class="project-actions text-right">
        <a class="btn btn-primary btn-sm" href="{{ path('detailscatservice',{'id':cat.id}) }}">
            <i class="fas fa-folder">
            </i>
            Détails
        </a>
        <a class="btn btn-info btn-sm" href="{{ path('updatecatservice',{'id':cat.id}) }}">
            <i class="fas fa-pencil-alt">
            </i>
            modifer
        </a>
        <a class="btn btn-danger btn-sm" href="{{ path('deletecatservice',{'id':cat.id}) }}">
            <i class="fas fa-trash">
            </i>
            supprimer
        </a>
    </td>
     </tr>
{% endfor %}
