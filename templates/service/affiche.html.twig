{% extends 'baseBack.html.twig' %}

{% block contentHeader %} Services {% endblock %}
{%  block contentMain  %}
    <section class="content">
        <!-- Default box -->
        <div class="card card-solid" >
            <div class="card-header">
                <a class="btn bg-gradient-info float-left " href="{{ path('Ajouterservice') }}">
                    <i class="far fa-plus-square "> ajouter
                    </i>

                </a>

                <div class="card-tools">
                    <div class="input-group input-group-sm" style="width: 150px;">
                        <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                        <div class="input-group-append">
                            <button type="submit" class="btn btn-default"><i class="fas fa-search"></i></button>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card-body pb-0">
                <p {% if services|length==0 %}>nothing up to data </p>
                <div  class="row d-flex align-items-stretch" >
                    {% else %}
                    <div  class="col-12 col-sm-6 col-md-4 d-flex align-items-stretch">
                        {% for s in services %}
                        <div class="card bg-light col-12 col-sm-6 col-md-4 d-flex align-items-stretch "  >
                            <div class="card-header text-muted border-bottom-0">
                                Categorie : {{ s.categorie.libelle }}
                            </div>
                            <div class="card-body pt-0">
                                <div class="row">
                                    <div class="col-7">
                                        <h2 class="lead"><b>libelle : {{ s.libelle }}</b></h2>
                                        <a href="" class="text-muted text-sm"><b>fournisseurs </b> </a>
                                        <ul class="ml-4 mb-0 fa-ul text-muted">

                                            <li {% for f in s.fournisseurs %} class="small"><span  class="fa-li"><i class="fas fa-user "></i>
                                                    {{ f.fourniseur }}
                                                </span>
                                            </li {% endfor %}>
                                        </ul>
                                    </div>
                                    <div class="col-5 text-center">
                                        <img src={{ asset("distAdmin/img/docdoc.png") }} alt="" class="img-circle img-fluid">
                                    </div>
                                </div>
                            </div>
                            <div class="card-footer">
                                <div class="text-right">
                                    <a href="{{ path('detailservice',{'id':s.id}) }}" class="btn btn-sm btn-primary">
                                        <i class="fas fa-asterisk"></i>details
                                    </a>
                                </div>
                            </div>
                        </div>z
                        {% endfor %}
                    </div >

                </div >
                {% endif %}
            </div>
            <!-- /.card-body -->
            <div class="card-footer">

            </div>
            <!-- /.card-footer -->
        </div>
        <!-- /.card -->

    </section>

{% endblock %}
