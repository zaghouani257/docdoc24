<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
<div class="bg-light p-1">
    <h2> &nbsp; &nbsp; &nbsp;DocDoc vous remercie pour votre confiance.</h2>
    <h2> &nbsp; &nbsp; &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Votre commande :</h2>
</div>
<table align="center" valign="center" class="example-wrapper">
    <thead>
    <tr>
        <th>Produit</th>
        <th>Prix</th>
        <th>Description</th>
        <th>Quantité</th>
        <th>Total</th>
    </tr>
    </thead>
    <tbody>
    {% for item in items %}
        <tr>
            <td>{{ item.produit.nom }}</td>
            <td>{{ item.produit.prix }} DT</td>
            <td>{{ item.produit.description }}</td>
            <td>{{ item.quantity }}</td>
            <td>{{ item.produit.prix * item.quantity }}</td>
            <td>
                <a href="{{ path('cart_remove', {'id': item.produit.id}) }}" class="btn btn-danger glyphicon glyphicon-trash btn-sm">
                    <i class="fas remove"></i>
                </a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
    <tfoot>
    <tr>
        <td colspan="3" class="text-right"> Total :</td>
        <td>{{ total }} DT</td>
        <td></td>
    </tr>

    </tfoot>
</table>
</body>
</html>
